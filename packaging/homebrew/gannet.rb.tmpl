# typed: false
# frozen_string_literal: true

# Generated by the release workflow in planesailingio/gannet — do not edit by hand.
class Gannet < Formula
  desc "Lightweight package manager for GitHub release binaries"
  homepage "https://github.com/planesailingio/gannet"
  version "__VERSION__"
  license "MIT"

  on_macos do
    on_arm do
      url "https://github.com/planesailingio/gannet/releases/download/__TAG__/gannet-__TAG__-aarch64-apple-darwin.tar.gz"
      sha256 "__SHA_DARWIN_ARM__"
    end
    on_intel do
      url "https://github.com/planesailingio/gannet/releases/download/__TAG__/gannet-__TAG__-x86_64-apple-darwin.tar.gz"
      sha256 "__SHA_DARWIN_X86__"
    end
  end

  on_linux do
    on_arm do
      url "https://github.com/planesailingio/gannet/releases/download/__TAG__/gannet-__TAG__-aarch64-unknown-linux-musl.tar.gz"
      sha256 "__SHA_LINUX_ARM__"
    end
    on_intel do
      url "https://github.com/planesailingio/gannet/releases/download/__TAG__/gannet-__TAG__-x86_64-unknown-linux-musl.tar.gz"
      sha256 "__SHA_LINUX_X86__"
    end
  end

  def install
    bin.install "gannet"
    bash_completion.install "completions/gannet.bash" => "gannet"
    zsh_completion.install "completions/_gannet"
    fish_completion.install "completions/gannet.fish"
  end

  test do
    assert_match version.to_s, shell_output("#{bin}/gannet --version")
  end
end
